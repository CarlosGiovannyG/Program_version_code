query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
acronym:$acronym,
fields:$fields,
where:$where,
pageSize:1000
  ){
    id,
    fields{
      key,
      value
    }
  }
}