query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
acronym:$acronym,
fields:$fields,
where:$where,
pageSize:100
  ){
    id,
    fields{
      key,
      value
    }
  }
}