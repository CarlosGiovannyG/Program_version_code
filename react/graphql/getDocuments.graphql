query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
acronym:$acronym,
fields:$fields,
where:$where,
pageSize:1800
  ){
    id,
    fields{
      key,
      value
    }
  }
}