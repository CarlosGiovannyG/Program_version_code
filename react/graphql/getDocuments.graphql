query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
  acronym:$acronym
  fields:$fields
  where:$where
  pageSize:1200
)
@context(providers:"vtex.store-graphql")
  {
    id,
    fields{
      key,
      value
    }
  }
}