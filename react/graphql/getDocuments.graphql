query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
acronym:$acronym,
fields:$fields,
where:$where
)
@context(providers:"vtex.store-graphql")
  {
    id,
    fields{
      key,
      value
    }
  }
}