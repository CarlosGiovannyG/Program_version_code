query GET_DOCUMENTS(
  $acronym:String!
  $fields:[String!]
  $where:String
){
  documents(
acronym:$acronym,
fields:$fields,
where:$where,
pageSize:1600
  ){
    id,
    fields{
      key,
      value
    }
  }
}